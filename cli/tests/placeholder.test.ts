import { describe, it } from 'vitest';

// Placeholder — CLI is a thin script (run-agent.ts + render.ts) and is covered
// by integration tests. Unit tests are added here as the module grows.
describe('cli', () => {
  it('placeholder', () => {
    // nothing to assert yet
  });
});
